{
  "name": "cronometro-vida-ministerio",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8",
    "jspdf": "^2.5.2"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
